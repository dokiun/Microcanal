#!/bin/bash
# Preserve authored initial fields and geometry. Remove only generated run output.
set -euo pipefail
cd "$(dirname "$0")"
: "${WM_PROJECT_DIR:?Load OpenFOAM first}"
source "$WM_PROJECT_DIR/bin/tools/CleanFunctions"
cleanTimeDirectories
cleanPostProcessing
