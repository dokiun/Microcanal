probesFluid
{
    type probes; libs (sampling); region fluid;
    writeControl timeStep; writeInterval 10;
    fields (p T.liquid T.gas alpha.gas U.liquid U.gas);
    probeLocations ((0.0001125 0 0.001) (0.0001125 0 0.005) (0.0001125 0 0.009));
}
probesSolid
{
    type probes; libs (sampling); region solid;
    writeControl timeStep; writeInterval 10;
    fields (T);
    probeLocations ((0 -0.000175 0.001) (0 -0.000175 0.005) (0 -0.000175 0.009));
}
phaseStatistics
{
    type fieldMinMax; libs (fieldFunctionObjects); region fluid;
    writeControl timeStep; writeInterval 10;
    fields (alpha.gas T.gas T.liquid p); location false;
}
